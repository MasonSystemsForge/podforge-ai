# podforge-ai

A lightweight prototype for starting a podcast planning flow from a user-entered topic.

## Run locally

Serve the repository root with any static file server, then open `index.html`.

```bash
python3 -m http.server 8000
```

Visit `http://localhost:8000`.

## Test

```bash
npm test
```
